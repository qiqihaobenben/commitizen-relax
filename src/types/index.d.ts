export declare interface CommanderOptions {
  force: boolean;
  config: "zh" | "en";
}

export declare interface InitOptions {
  save: boolean;
  saveDev: boolean;
  saveExact: boolean;
  force: boolean;
  yarn: boolean;
  dev: boolean;
  exact: boolean;
}
