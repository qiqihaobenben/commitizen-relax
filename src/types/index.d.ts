export declare interface CommanderOptions {
  force: boolean;
  language: "zh" | "en";
  adapter: string;
}

export declare interface InitOptions {
  save: boolean;
  saveDev: boolean;
  saveExact: boolean;
  force: boolean;
  yarn: boolean;
  dev: boolean;
  exact: boolean;
}
